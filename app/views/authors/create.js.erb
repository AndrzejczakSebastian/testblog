<% if @author.persisted? %>
  $('.authors').append('<%= j render('author', author: @author) %>');
<% else %>
  alert('<%= j @author.errors.full_messages.join(', ') %>');
<% end %>


$('.new-autor').find('input[type="text"], input[type="number"]').val('');
